{
  "name": "whatsapp-bulk-sender",
  "version": "1.0.0",
  "description": "App para envio automático de mensagens via WhatsApp",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . \"Nery Auto Messenger\" --platform=win32 --arch=x64 --icon=icon.ico --out=dist --overwrite"

  },
  "dependencies": {
    "express": "^4.18.2",
    "helmet": "^8.1.0",
    "multer": "^1.4.5-lts.1",
    "puppeteer": "^24.10.0",
    "socket.io": "^4.8.1",
    "whatsapp-web.js": "^1.20.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "electron": "^26.0.0",
    "electron-packager": "^16.0.0"
  }
}
